import { ShowIfDemoDirective } from './show-if-demo.directive';

describe('ShowIfDemoDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowIfDemoDirective();
    expect(directive).toBeTruthy();
  });
});
